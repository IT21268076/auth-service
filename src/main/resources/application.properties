# Application
spring.application.name=auth-service
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/auth_db?createDatabaseIfNotExist=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=bhtd1996213
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT Configuration
jwt.secret=secureJwtSecretKeyThatIsAtLeast32CharactersLong
jwt.expiration=86400000

# Security
spring.security.filter.order=10

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.example.authservice=DEBUG